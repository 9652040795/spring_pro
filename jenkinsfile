pipeline {
  agent any
  environment {
        MYJOB = 'DevOps1'
        BUILD = 'PIPELINE1'
  }
  stages {
   stage('Clone repository') {
      steps {
        checkout scm
        echo "Sudhakar"
        echo "$MYJOB"
        echo "$BUILD"
      }
    }
 }
  }     
  

